# Sadhana — Google Sheets (legacy)

**Runtime status:** Sadhana UI uses **Cosmos** via `POST /api/sadhana`.  
This doc is historical for sheet layout / Apps Script reference.

See `docs/COSMOS_DATA_MIGRATION.md` and `src/sadhana/google-apps-script-sadhana.js` for the old behavior that was ported.
