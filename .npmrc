# Use the public npm registry so CI (Azure Static Web Apps, GitHub Actions) can install
# dependencies. Corporate mirrors (e.g. internal Nexus) are not reachable from cloud builders.
registry=https://registry.npmjs.org/
